# Optional — URL of the Google Earth Engine tile proxy (see /server/README.md).
# Leave unset to run Sentinel without the raster overlay (the control hides
# itself). Defaults to "/api/gee-tiles" (same-origin serverless function).
VITE_GEE_TILES_URL=
